import { browser } from '$app/environment';

export const load = async () => {
  // Any server-side or universal load logic can go here
  return {
    // You can return data that will be available to the layout and all child routes
  };
};
